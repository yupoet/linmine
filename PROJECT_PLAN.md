# Lin Mine 项目计划书

版本：v1.2  
日期：2026-09-04  
状态：待立项评审  
目标：先验证“点击—连锁—取舍—再来一局”是否成立，再扩展长期养成与运营系统。

### 变更记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v1.0 | 2026-09-04 | 初稿 |
| v1.1 | 2026-09-04 | 修正 License 事实、缩小 MVP、拆出运营层、补齐工程门禁、改用人周排期（详见 2.2） |
| v1.2 | 2026-09-04 | M0 改为多轮小样本验证、M1 增加决策落地缓冲、补风险复核节奏与决策逾期兜底、提前商标检索、补最小美术资产清单和事件 payload 约定（详见 2.3） |

---

## 1. 执行摘要

**Lin Mine** 是一款移动 Web 优先的竖屏 3D 点击挖掘游戏。玩家在有限镐耐久内向下推进，通过矿物、特殊方块和卡牌组合制造连锁收益，并用局外升级解锁更深矿层。

- 核心体验：单指可玩、一次操作立即反馈、2–4 分钟一局、失败后有清晰的下一局提升方向。
- 首发平台：移动 Web；桌面浏览器只保证可玩，不单独设计交互。
- 技术基线：Three.js + TypeScript + Vite，不引入物理引擎或完整游戏引擎。
- 产品策略：先做 1 座岛的可发布 MVP；Energy、广告、全球排行榜、每日活动等运营能力通过验证后再做。
- 工期判断：MVP 约 **15–19 人周**（含 M1 开头 0.5 人周的 M0 决策落地缓冲）。1 名全栈开发约 15–19 周；2 人（程序 + 美术/内容）约 10–13 个日历周。远端后端、商业 SDK 和大批量原创美术不含在内。

### 立项成功条件

MVP 不以“功能全部做完”为成功，而以以下证据为准：

1. 新玩家无需口头指导即可完成第一局；
2. 点击、破坏、掉落和连锁反馈足够清晰；
3. 玩家理解“耐久换深度/收益”的取舍，并愿意主动再玩一局；
4. 目标移动设备上稳定运行，存档可升级、核心规则可自动测试和批量模拟；
5. 全部名称、视觉、音频、文案及数值表达为原创或具有可追踪授权。

## 2. 当前基线与本次评审结论

### 2.1 仓库现状

截至 2026-09-04，仓库包含 `package.json`、`package-lock.json`、`tsconfig.json` 和 `vite.config.ts`，但尚无 `index.html` 或 `src/`。因此当前应描述为“**依赖和构建配置已建立**”，不是“可运行脚手架已就位”。

- 声明依赖：Three.js `^0.169.0`、TypeScript `^5.6.3`、Vite `^5.4.10`；
- 锁文件当前解析：Three.js `0.169.0`、TypeScript `5.9.3`、Vite `5.4.21`；
- `npm run typecheck` 可通过；
- `npm run build` 当前失败：缺少 `index.html` 入口；
- 当前目录不是 Git 仓库，尚无提交历史或 CI。

以上缺口纳入 M−1，不在计划中假设已经完成。依赖是否升级在 M−1 做一次兼容性审计；没有明确收益时保持锁定版本，避免把升级风险混入玩法验证。

### 2.2 对 v1.0 的主要修正

1. **修正 License 事实**：`kiforsbe/pocketminer` 当前未标注 License，不能按 MIT 复用；核心参考中只有 `alektron/ChunkMinerGame` 被 GitHub 明确识别为 MIT。
2. **缩小 MVP**：首版由 3–4 座岛、20 张卡、6 档宝箱收敛为 1 座岛、8 张卡、2 档宝箱和 1 条工具线。
3. **拆出运营层**：Energy、广告、付费货币、全球排行榜和每日活动不再阻塞 MVP 发布。
4. **补齐工程门禁**：确定性随机、回放、流式矿层、存档迁移、性能基线、CI 和 Definition of Done。
5. **修正排期表达**：使用人周并列出人员假设，不再用“1–2 人都是 12 周”的模糊估算。
6. **区分事实与假设**：对参考游戏的网格列数、滚屏规则、箱子清单等，不在验证前写成 Lin Mine 的既定需求。

### 2.3 对 v1.1 的主要修正

1. **M0 门禁改为多轮验证**：n=8 单轮通过/失败容易被噪声主导，改为 2–3 轮、每轮 5–8 人，以连续两轮达标为准。
2. **排期补返工缓冲**：4.4 节待验证项的决策可能改变 M1 的流式矿层与结算实现，M1 开头显式预留 0.5 人周做“M0 决策落地”，总估算由 14–18 人周调整为 15–19 人周。
3. **风险登记补复核节奏**：明确每个里程碑门禁评审时复核风险登记，由项目负责人判定触发。
4. **待决策清单补逾期兜底**：截止点未决策则自动生效默认方案，不允许悬置。
5. **商标/域名检索提前**：由“M2 结束”改为“M1 结束前启动、M2 结束前锁定”，避免 M3 发布被外部流程卡住。
6. **补最小美术资产清单**：M−1 交付一份方块/角色/卡牌/UI 的数量清单，并划清占位与最终美术边界，防止“原创美术”成为范围蔓延入口。
7. **事件模型补 payload 约定**：8.1 仅定义事件名，payload schema 在 M1 定稿，避免实现期反复改接口。

## 3. 开源调研与使用边界

数据为 2026-09-04 的 GitHub 快照；Stars 会变化，只用于判断社区规模，不用于证明技术或商业可行性。

| 仓库 | Stars | GitHub License | 可借鉴内容 | 使用边界 |
|---|---:|---|---|---|
| [kiforsbe/pocketminer](https://github.com/kiforsbe/pocketminer) | 0 | 未标注 | 限时轮次、拾取、局间商店、确定性模拟/轨迹工具的思路 | 默认保留全部权利；不复制代码、素材或文案 |
| [tiwu/gemdigger](https://github.com/tiwu/gemdigger) | 0 | 未标注 | 纯规则与 DOM/渲染分离、Vitest 单测、可选排行榜后端 | 默认保留全部权利；不复制实现 |
| [alektron/ChunkMinerGame](https://github.com/alektron/ChunkMinerGame) | 65 | MIT | 小型 3D 游戏的相机、粒子和极简范围控制 | 如使用代码，保留 MIT 许可与版权声明 |
| [llakssz/Dr-Driller](https://github.com/llakssz/Dr-Driller) | 13 | 未标注 | 向下推进和资源压力的题材参考 | 未完成且无许可，仅观察公开行为与概念 |
| [Gertkeno/to-the-core](https://github.com/Gertkeno/to-the-core) | 3 | 未标注 | 极简竖井挖掘的范围参考 | 不复制代码或素材 |

调研结论应谨慎表述为：**未发现可直接 fork、且与目标玩法高度同构的成熟开源实现**。这些项目说明若干子问题有公开先例，但不能证明本项目的移动 Web 性能、玩法手感或 12 周交付一定成立。

项目建立 `THIRD_PARTY_NOTICES.md` 和素材来源台账。无明确 License 的仓库按“不可复用”处理；即使是 MIT 代码，也只在确有维护收益时引入，不默认搬运。

## 4. 产品范围

### 4.1 核心循环

```text
选择关卡与 3 张卡牌
  → 消耗本局镐耐久，点击可到达的方块
  → 矿工自动移动并挖掘
  → 收集矿物，触发炸药/箭块等连锁
  → 达到目标深度则胜利；耐久耗尽则失败
  → 结算现金与卡牌进度
  → 升级后再开一局
```

规则原则：

- 一次点击对应一个明确意图；移动本身不消耗镐耐久；
- 硬块可一次点击消耗多点耐久，UI 必须在点击前显示预计成本；
- 只能挖掘与可达空间相邻的方块，非法目标必须即时反馈；
- 连锁破坏默认不额外消耗镐耐久；是否伤害玩家作为原型变量测试，不预设为正式规则；
- 每局由 seed + 配置版本决定，输入命令可记录和重放。

### 4.2 MVP（Must）

- 1 座主题岛、5 个递进关卡；每局目标时长 2–4 分钟；
- 可配置网格宽度，逻辑矿层按行生成，渲染只保留可视窗口；
- 6 类基础方块：土、石、2 种矿石、不可破地层、出口/目标；
- 4 类特殊方块：炸药、定向箭、治疗/修复、金库；
- 耐久、深度、现金、掉落、胜负和结算；
- 8 张卡牌，覆盖生成率、收益、耐久、连锁四种效果族；每局选 3 张；
- 2 档宝箱、1 条镐升级线、单版本局外经济；
- 本地存档、版本迁移、重新开始和调试清档；
- 触摸、鼠标输入，基础音效/震屏/粒子及静音；
- 新手引导、错误兜底、最小事件埋点接口（默认无第三方服务）。

### 4.3 MVP 明确不做（Won't）

- Energy 恢复、激励广告、内购和付费续命；
- 红宝石等硬货币；
- 全球/好友排行榜、账号和云存档；
- 每日活动、赛季、工厂、成就、社交；
- 3–4 座岛、6 档宝箱、10 星卡牌和 20+ 卡牌；
- iOS/Android/微信小游戏包壳；
- 复杂角色养成、动态光照、实时物理、多人联机。

这些能力进入验证后路线图，不能以“顺手做”加入当前迭代。

### 4.4 玩法待验证项

下列内容是设计变量，不是对参考游戏的既定复刻：

| 问题 | M0 实验 | 决策标准 |
|---|---|---|
| 网格宽度 | 5、6、7 列 A/B 手感测试 | 误触率、可读性、决策空间 |
| 压迫机制 | 无滚屏 vs 缓慢滚屏 | 是否增强节奏且不掩盖耐久主压力 |
| 点击规则 | 仅相邻可挖 vs 自动寻路后挖 | 操作预期、等待时间、误操作率 |
| 连锁反噬 | 无伤害 vs 固定耐久伤害 | 惊喜是否大于挫败 |
| 单局长度 | 2、3、4 分钟目标 | 完成率、重玩意愿、移动端中断率 |

M0 结束即冻结上述规则；未获得证据时采用实现更简单、反馈更清晰的方案。

## 5. 技术方案

### 5.1 目录与依赖方向

```text
src/
  core/          纯 TypeScript 规则：网格、命令、结算、生成、寻路、卡牌效果
  config/        方块、关卡、卡牌、经济表；含 schema 与版本号
  app/           游戏状态机与用例编排：启动、单局、结算、局外升级
  render/        Three.js 场景、实例池、相机、角色表现、特效
  ui/            DOM HUD、菜单、引导、可访问性提示
  platform/      输入、存档、音频、时钟、埋点接口
  devtools/      seed 输入、回放、数值模拟和性能面板
  main.ts
tests/
  unit/          core/config
  integration/   app + 存档 + 输入
  smoke/         构建产物与关键流程
```

依赖只能向内：`render/ui/platform → app → core/config`；`core` 不导入 Three.js、DOM、localStorage 或真实时钟。

### 5.2 关键工程决策

- **确定性核心**：随机数、时钟和 ID 生成器均注入；一次玩家操作生成 command，规则输出 event，便于重放、调试和批量平衡。
- **矿层流式化**：逻辑只保留必要历史和前方缓冲区；渲染只创建相机附近行，避免 Endless 随深度无限占用内存。
- **实例化渲染**：按几何/材质族维护 `InstancedMesh` 池和“网格坐标 ↔ 实例索引”映射；删除用 swap-remove 或空闲槽复用，不假设实例可以直接删除。
- **寻路约束**：对可站立空格做 BFS；目标方块需存在可达的相邻工作位。设置最大搜索节点和取消令牌，新点击可替换未执行命令。
- **效果管线**：基础值 → 加法 → 乘法 → 上下限 → 结算；排序规则固定，卡牌不得直接修改任意全局状态。
- **存档安全**：存档包含 `schemaVersion`、配置版本和校验；读取失败回退到备份并提示，迁移函数逐版本测试。
- **生命周期**：处理 `visibilitychange`、音频解锁、横竖屏切换和 WebGL context lost；后台暂停，不以墙钟推进单局。
- **范围克制**：MVP 不上 ECS、不上物理引擎、不做后端；出现真实复杂度证据后再评估。

### 5.3 性能预算

M−1 指定至少 1 台低端和 1 台主流 Android、1 台 iPhone 作为测试基线。没有设备名单前，“中端手机 60fps”不可验收。

| 指标 | MVP 目标 | 最低门槛 |
|---|---:|---:|
| 交互期间帧率 | 60 FPS | p95 帧时长 ≤ 33 ms（约 30 FPS） |
| 点击到视觉/声音反馈 | p95 ≤ 100 ms | p95 ≤ 150 ms |
| 首次可操作时间（缓存冷启动） | ≤ 3 s | ≤ 5 s |
| JS 峰值堆内存 | ≤ 120 MB | ≤ 180 MB |
| 首包 gzip（不含按需音频） | ≤ 1.5 MB | ≤ 2.0 MB |
| 30 分钟连续游玩 | 无持续内存增长 | 无崩溃或 WebGL 丢失未恢复 |

指标通过固定 seed 的基准关卡测量，并记录设备、浏览器版本、构建 commit 和采样方式。

## 6. 交付计划与门禁

估算采用 **1 人周 = 5 个有效工作日**，包含开发、自测和评审，不含等待外部素材/账号的时间。每个里程碑通过门禁后再进入下一阶段。

| 阶段 | 主要交付 | 验收门禁 | 估算 |
|---|---|---|---:|
| **M−1 基线** | 可运行入口、Git/CI、Vitest、代码规范、设备矩阵、原创性台账、最小美术资产清单 | `npm run build`、类型检查、测试在 CI 全绿；手机出现可交互画面 | 0.5–1 人周 |
| **M0 手感原型** | 网格、流式生成、点击/寻路/挖掘、耐久、胜负、临时美术、3 个 seed | 2–3 轮内部测试，每轮 5–8 人；连续两轮均满足 ≥60% 无需解释完成一局、≥50% 主动再开一局；玩法待验证项完成决策并记录证据 | 2–3 人周 |
| **M1 纵向切片** | 0.5 人周“M0 决策落地”检查点（按 4.4 决策调整流式矿层/结算/寻路设计），随后 1 关完整流程、6 基础块、4 特殊块、结算、基础 FX/音频、回放工具 | 1000 个 seed 模拟无非法状态；关键流程集成测试通过；达到性能最低门槛 | 3.5–4.5 人周 |
| **M2 元循环** | 5 关、8 卡、2 宝箱、镐升级、经济、存档/迁移、新手引导 | 新档/旧档/损坏档测试通过；1 万局模拟无负资产、死锁或收益溢出；完整循环可连续玩 30 分钟 | 4–5 人周 |
| **M3 MVP 发布候选** | 原创视觉音频替换、兼容性、埋点接口、部署、隐私/致谢页面、缺陷清理 | 30 名封测：引导完成率 ≥75%，同会话第二局开启率 ≥50%；测试会话无崩溃率 ≥99%；P0/P1 缺陷为 0 | 4–5 人周 |

合计 14–18.5 人周，计划承诺按 **15–19 人周** 计（多出的部分为未分配缓冲，用于吸收 M0 决策带来的返工，不得用于新增范围）。两人并行仅在职责可拆分且素材及时交付时缩短日历时间，不能简单除以二。

**最小美术资产清单（M−1 交付，M0 结束前冻结数量）**：列出方块（6 基础 + 4 特殊）、角色（1 个矿工 + 2–3 个动作）、卡牌图标（8 张）、宝箱（2 档）、HUD/菜单/引导所需 UI 元素的数量，并对每项标注“占位”或“最终”。M0–M2 只允许使用占位资产，最终资产替换集中在 M3；清单外的新增美术需求按范围变更处理。

### M4 验证后路线图（不计入 MVP）

按数据逐项立项，不打包承诺：

1. 内容扩展：更多岛、卡牌、宝箱、文物和工具；
2. 留存系统：Energy、每日挑战、成就；
3. 服务端：账号、云存档、排行榜与反作弊；
4. 商业化：广告/内购、年龄分级、隐私同意、商店合规；
5. 平台化：PWA、原生包壳或微信小游戏适配。

## 7. 质量策略

### 7.1 自动化测试

- 单元测试：耐久、掉落、生成权重、卡牌排序、经济、寻路边界、存档迁移；
- 性质/批量测试：任意合法 command 后状态满足不变量；同 seed + 同命令得到同结果；
- 集成测试：开局 → 挖掘 → 胜负 → 结算 → 升级 → 再开局；
- Smoke：生产构建可加载、无控制台致命错误、触摸首局可完成；
- 回归样本：为每个已修复的 P0/P1 玩法 bug 固化 seed 和命令序列。

### 7.2 Definition of Done

功能只有同时满足以下条件才算完成：

1. 规则和配置有验收样例，边界条件有自动化测试；
2. 触摸与鼠标均验证，失败/禁用状态有用户可见反馈；
3. 不破坏确定性回放和存档迁移；
4. 性能面板无明显回退，必要时附基准记录；
5. 使用的素材和第三方代码已登记来源与 License；
6. 文档、配置 schema 和调试入口同步更新。

## 8. 数据与平衡

### 8.1 最小事件模型

在不接第三方分析服务时先定义事件接口：`run_start`、`first_input`、`block_mined`、`chain_triggered`、`run_end`、`upgrade_buy`、`tutorial_step`、`error`。开发环境可导出 JSON；封测是否采集须先完成隐私说明和用户同意设计。

各事件的 payload schema 在 M1 定稿并纳入 `config/` 的版本化 schema；M0 阶段只保证事件名与公共字段（`seed`、`configVersion`、`runId`、`t`）稳定，不承诺细分字段，避免实现期反复改接口。

### 8.2 平衡流程

1. 用配置表定义关卡预算：可用耐久、必经硬度、预期连锁节省、目标收益；
2. 固定一组基准 seed，保证改表前后可比较；
3. 批量模拟发现不可解地图、收益爆炸和单一卡牌支配；
4. 人工测试验证模拟无法衡量的清晰度、节奏和爽感；
5. 每次调参记录配置版本、假设、样本和结论。

MVP 目标是“存在多种可理解选择”，不是一次性做出长期经济。10 星卡牌、硬货币和广告加入前必须重新建模。

## 9. 风险登记

| 风险 | 概率/影响 | 缓解措施 | 触发条件与动作 |
|---|---|---|---|
| 核心操作不够爽 | 高/高 | M0 使用临时素材尽早测点击、连锁和节奏 | M0 未过重玩门禁：暂停元系统，继续迭代或终止项目 |
| 玩法范围膨胀 | 高/高 | Must/Won't 清单、里程碑变更需交换范围 | 新需求 >2 人日：必须移除等量范围或调整排期 |
| 移动端性能不足 | 中/高 | 可视窗口、实例池、固定 seed 基准、真机早测 | M1 未达 30 FPS 门槛：先降可视行数/特效，再评估架构 |
| 数值不可控 | 高/中 | 确定性模拟、效果顺序、配置版本 | 出现必选卡或收益跨度 >3 倍：冻结内容，先修模型 |
| 存档损坏/版本不兼容 | 中/高 | schema、备份、迁移测试、导出调试档 | 发现无法恢复的有效存档：阻断发布 |
| 版权/商标风险 | 中/高 | 原创资产、来源台账、独立命名；发布前做商标检索 | 视觉/名称与参考作品易混淆：替换后再发布 |
| 单人关键路径过长 | 高/中 | 先灰盒、按人周排期、内容模板化 | 连续两个里程碑偏差 >25%：砍内容而非压缩 QA |
| 后端/商业化带来合规负担 | 中/高 | MVP 不做，独立立项 | 决定接入时新增隐私、支付、未成年人和平台审核工作流 |

**复核节奏**：每个里程碑门禁评审时由项目负责人复核本表，逐项确认是否触发、缓解措施是否仍有效，并更新概率/影响；里程碑之间若出现触发条件，由发现者当日记录、负责人 2 个工作日内判定。触发后执行的动作与结论追加到风险行，不覆盖历史。

## 10. 待决策清单

**逾期规则**：到达截止点仍未做出决策时，默认方案自动生效并记录为“默认生效”，后续如需推翻按范围变更处理；不允许决策悬置到下一个里程碑。

| 决策 | 截止点 | 默认方案 |
|---|---|---|
| 目标测试设备与最低浏览器版本 | M−1 结束 | 先覆盖近 3 年主流 Safari/Chrome，具体机型以现有设备为准 |
| 5/6/7 列、是否滚屏、连锁反噬 | M0 结束 | 6 列、无滚屏、无反噬 |
| 角色是纯表现还是带技能 | M0 结束 | MVP 纯表现，避免与卡牌系统重叠 |
| 美术来源与预算 | M0 结束 | 按 M−1 最小美术资产清单，原创低多边形占位迭代，禁止抓取参考游戏素材 |
| 封测分发与数据采集方式 | M2 结束 | 静态部署 + 匿名本地导出，不默认接第三方 SDK |
| MVP 上线域名和产品名称 | M1 结束前启动商标/域名检索，M2 结束前锁定 | `Lin Mine` 仅作工作名；检索发现冲突则在 M2 内更名，不推迟到 M3 |

## 11. 下一步（开工顺序）

1. 完成 M−1：补 `index.html`、`src/main.ts`、Vitest 和 CI，使构建真正可运行；同步产出最小美术资产清单；
2. 选定 3 台性能基线设备，并记录浏览器版本；
3. 用灰盒方块完成 M0，不制作卡牌、商店或正式美术；
4. 建立可复用的可用性测试脚本和玩法变量记录表，按轮次（每轮 5–8 人）记录结果，M0 至少跑 2 轮；
5. M0 门禁通过后再批准 M1–M3 的完整投入；M1 开工第一步是把 4.4 的决策落到设计上，再写代码。

---

本计划的核心约束是：**先证明一局好玩，再证明成长有效，最后才证明运营可持续。**
